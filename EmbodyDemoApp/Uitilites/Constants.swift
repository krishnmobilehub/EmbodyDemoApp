//
//  Constants.swift
//  EmbodyDemoApp
//

import Foundation

// Video Name
struct VideoData {
    static let jsonFile = "Video"
}

struct Message {
    static let noVideoUrlAvailable = "No video url available"
    static let jsonNotParse = "No video json available"
    static let noCameraAccessMessage = "Please go to setting and enable device camera permission."
}

struct Title {
    static let setting = "Setting"
    static let alertTitle = "EmbodyDemoApp"
    static let errorTitle = "Error"
}

struct AlertButton {
    static let ok = "OK"
}
